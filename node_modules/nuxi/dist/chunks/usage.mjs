import { c as cyan } from '../shared/nuxi.3d7f34cc.mjs';
import { s as showHelp } from '../shared/nuxi.b9f0a96a.mjs';
import { d as defineNuxtCommand, c as commands } from '../shared/nuxi.b617cd0e.mjs';
import 'tty';

const usage = defineNuxtCommand({
  meta: {
    name: "help",
    usage: "nuxt help",
    description: "Show help"
  },
  invoke(_args) {
    const sections = [];
    sections.push(`Usage: ${cyan(`npx nuxi ${Object.keys(commands).join("|")} [args]`)}`);
    console.log(sections.join("\n\n") + "\n");
    showHelp({});
  }
});

export { usage as default };
